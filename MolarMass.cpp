// MolarMass.cpp
// This program should loop through the csv file Atomic Masses.csv and sum the masses of elements
#include <iostream>
#include <string>
#include <fstream>


int main()
{
	ifstream in("AtomicMasses.cpp"); //In an ideal world this opens file
	double runningSum; 

	for () //For each element in the Smile, this will see if it is present in the csv file
		getline()
		/* I need to find a way to deal with the two letter abbreviations of elements in the search function
		   maybe something along the lines of "if [next value] is lowercase, then search both letters"*/


	return 0;
}
